# macOS writes one of these into every folder you open in Finder.
.DS_Store

# Editor and OS noise
Thumbs.db
*.swp
.idea/
.vscode/

# Installed only to run the Playwright render checks. Never deployed.
node_modules/
package-lock.json
